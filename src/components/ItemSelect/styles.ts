import { SxProps } from "@mui/material";

export const selectStyles: Record<string, SxProps> = {
  box: {
    display: { xs: "none", sm: "flex" },
    width: "100%"
  },
  select: {
    textAlign: "left"
  },
  menuItem: {
    textAlign: "left"
  }
};
